# Database (SQLite)
DATABASE_URL="file:/home/24hournews/db/custom.db"

# Node environment
NODE_ENV="production"

# Port the app runs on internally
PORT=3003

# Optional: JWT Secret for auth (auto-generated if empty)
# JWT_SECRET="your-secret-key-here"